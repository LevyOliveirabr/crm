# crm-fled — variáveis de ambiente (copie para .env.local e preencha).
#
# ATENÇÃO: valores provisórios com base no padrão Supabase.
# Precisam ser reconciliados com a SPEC seção 2 assim que o arquivo SPEC.md
# estiver disponível neste projeto.

# Supabase (cliente / browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (servidor) — nunca expor no browser
SUPABASE_SERVICE_ROLE_KEY=
